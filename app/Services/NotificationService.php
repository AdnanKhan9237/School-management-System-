<?php

declare(strict_types=1);

namespace App\Services;

class NotificationService
{
    /**
     * Placeholder for dispatching notifications (mail, SMS, in-app) which are
     * typically queued via Horizon. Kept minimal to scaffold the service layer.
     */
    public function notify(string $channel, array $payload): void
    {
        // Intentionally left as a stub for feature implementation.
    }
}
